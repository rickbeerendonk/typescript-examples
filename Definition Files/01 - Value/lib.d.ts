export declare const pi: number;

export declare let val: string;

export function double(val: number): number;

enum sex {
  m = 'male',
  f = 'female',
  t = 'third'
}

export class Person {
  name: string;
  sex: sex;
}
